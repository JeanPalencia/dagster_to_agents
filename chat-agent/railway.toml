[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPaths = ["chat-agent/**", "Dockerfile.chat-agent", ".claude/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
